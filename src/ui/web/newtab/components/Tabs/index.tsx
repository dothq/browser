import React from "react";

import { observer } from "mobx-react";
import { StyledTabs } from "../../../../app/components/Tabs/style";

export const Tabs = observer(() => (
    <StyledTabs>

    </StyledTabs>
))