export const pointer = () => `
    user-select: none;
    cursor: pointer;
    user-drag: none;
    app-region: no-drag;
  `;

export const defaultCursor = () => `
    user-select: none;
    cursor: default;
    user-drag: none;
    app-region: no-drag;
  `;
