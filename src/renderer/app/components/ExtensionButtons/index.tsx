import React from "react";
import { StyledExtensionButtons } from "./style";

export const ExtensionButtons = ({ children }: { children: any }) => (
    <StyledExtensionButtons>
        {children}
    </StyledExtensionButtons>
)