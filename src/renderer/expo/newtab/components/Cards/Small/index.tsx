import React from "react";

import { observer } from "mobx-react";
import { StyledSmallCard } from "./style";

export const SmallCard = observer(() => (
    <StyledSmallCard>

    </StyledSmallCard>
))