import styled from "styled-components";

export const StyledLargeCard = styled.div`
    width: 500px;
    height: 328px;

    background: linear-gradient(180.32deg, #ffffff00 0%, rgb(0, 0, 0) 100%);
    box-shadow: 0px 1px 22px rgba(0, 0, 0, 0.25);
    border-radius: 4px;

    margin: 26px;
`;